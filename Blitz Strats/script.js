function about() {
	window.scrollTo({top: window.innerHeight, behavior: "smooth"});
}

function onion() {
	window.scrollTo({top: window.innerHeight, behavior: "smooth"});
}

function tanks() {
	window.scrollTo({top: window.innerHeight * 1.65, behavior: "smooth"});
}

function spot() {
	window.scrollTo({top: window.innerHeight, behavior: "smooth"});
}

function pos() {
	window.scrollTo({top: window.innerHeight * 2.3, behavior: "smooth"});
}

function cover() {
	window.scrollTo({top: window.innerHeight, behavior: "smooth"});
}

function poking() {
	window.scrollTo({top: window.innerHeight * 2.3, behavior: "smooth"});
}

function shell() {
	window.scrollTo({top: window.innerHeight, behavior: "smooth"});
}

function armor() {
	window.scrollTo({top: window.innerHeight * 3.225, behavior: "smooth"});
}